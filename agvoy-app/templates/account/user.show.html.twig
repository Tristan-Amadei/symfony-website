{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block main %}
{% if is_granted('ROLE_ADMIN') or (is_granted('ROLE_USER') and user.id == app.user.id) %}
    <h1>User</h1>

    <table class="table">
        <tbody>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('user_account_edit', {'id': user.id}) }}">Edit</a>

    {{ include('account/_delete_form.html.twig') }}
{% else %}
<p>Ceci n'est pas votre page utilisateur, revenez à l'accueil du site en cliquant sur 'Travel Agency' sur la barre supérieure</p>
{% endif %}
{% endblock %}

{% block footer %} 
  <br/>
  <br/>
    &copy; Copyright 2021 by T.A</a>
{% endblock %}
